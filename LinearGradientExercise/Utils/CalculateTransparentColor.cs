using System;
using System.Collections.Generic;
using System.Linq;
using System.Web;
using LinearGradientExercise.Models;

namespace LinearGradientExercise.Utils
{
    public class CalculateTransparentColor
    {
        //public Tuple<int, int, int> caculateTransparentColor(string foregroundColor, string backgroundColor, float opacity)
        //{
        //    if (opacity < 0.0 || opacity > 1.0)
        //    {
        //        throw new ArgumentException("assertion, opacity should be between 0 and 1");
        //    }

        //    var foregroundRGB = new RgbHexColor(foregroundColor);
        //    var backgroundRGB = new RgbHexColor(backgroundColor);
        //    var finalRed = Math.Round(foregroundRGB.r * (1 - opacity) + backgroundRGB.r * opacity);
        //    var finalGreen = Math.Round(foregroundRGB.g * (1 - opacity) + backgroundRGB.g * opacity);
        //    var finalBlue = Math.Round(foregroundRGB.b * (1 - opacity) + backgroundRGB.b * opacity);

        //    return colorRGBToHex(finalRed, finalGreen, finalBlue);
        //}
    }
}